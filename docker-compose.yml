version: "3.8"
services:
  web:
    build: .
    image: rebuilding-milo-rag:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./samples:/app/samples
    restart: unless-stopped
